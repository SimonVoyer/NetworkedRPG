class Zelda {

	constructor(){
		//initializing sprite
	// let columnCount = 5;
	// let rowCount = 4;
	// let refreshDelay = 100; 			// msec
	// let loopColumns = true; 			// or by row?
	// let scale = 1.0;
	// sprite = new TiledImage("images/linkSprites.png", columnCount, rowCount, refreshDelay, loopColumns, scale, null);
	// sprite.changeRow(3);				// One row per animation
	// sprite.changeMinMaxInterval(1, columnCount); 	// Loop from which column to which column?
	 }

	tick(){

	}

}